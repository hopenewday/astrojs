---
/**
 * Example page demonstrating React components in Astro.js
 */
import BaseLayout from '../../layouts/BaseLayout.astro';
import ReactCounter from '../../components/common/ReactCounter.jsx';

const title = 'React Components Example';
const description = 'This page demonstrates how to use React components within Astro.js';
---

<BaseLayout title={title} description={description}>
  <div class="container mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold mb-6">{title}</h1>
    
    <p class="mb-8">
      This page demonstrates how to use React components within Astro.js. 
      The counter below is a React component that is hydrated on the client side.
    </p>
    
    <div class="mb-8">
      <ReactCounter client:load initialCount={5} />
    </div>
    
    <div class="bg-blue-50 p-4 rounded-lg border border-blue-200">
      <h2 class="text-xl font-bold mb-2">How it works</h2>
      <p class="mb-4">
        Astro.js allows you to use components from various UI frameworks like React, Vue, and Svelte.
        The <code class="bg-gray-100 px-1 rounded">client:</code> directive tells Astro how to hydrate the component on the client side.
      </p>
      
      <h3 class="text-lg font-bold mt-4 mb-2">Available client directives:</h3>
      <ul class="list-disc pl-6 space-y-2">
        <li><code class="bg-gray-100 px-1 rounded">client:load</code> - Hydrate the component as soon as the page loads</li>
        <li><code class="bg-gray-100 px-1 rounded">client:idle</code> - Hydrate when the browser is idle</li>
        <li><code class="bg-gray-100 px-1 rounded">client:visible</code> - Hydrate when the component is visible in the viewport</li>
        <li><code class="bg-gray-100 px-1 rounded">client:media</code> - Hydrate when a CSS media query is matched</li>
      </ul>
      
      <div class="mt-6 p-3 bg-green-50 border border-green-200 rounded-lg">
        <p class="font-medium">Ready for more advanced examples?</p>
        <p class="mb-2">Check out our <a href="/examples/advanced-react-components" class="text-blue-600 hover:underline font-medium">Advanced React Components</a> page to see:</p>
        <ul class="list-disc pl-6 space-y-1 text-sm">
          <li>TypeScript React components with theming</li>
          <li>State management with React Context API</li>
          <li>Advanced hooks usage (useCallback, useMemo)</li>
          <li>Data fetching from external APIs</li>
        </ul>
      </div>
    </div>
  </div>
</BaseLayout>